{
  "name": "the-css-handbook",
  "version": "1.0.0",
  "main": "build.js",
  "type": "module",
  "scripts": {
    "build": "rimraf build && node build.js && prettier --write build"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "marked": "^15.0.1"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "rimraf": "^5.0.10"
  }
}
